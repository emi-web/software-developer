// and , or, not

// and =&&
let older = true;
let subs = true;
// console.log( true && true);
// console.log( false && true);
console.log('operator and',older && subs);

//or ||
console.log('operator or',older || subs);

//not !
console.log('operator not', !subs);
let catalogeInfan = !mayor;